###############################################################################
# debug.conf — not sourced automatically
# To enable verbose logging, temporarily add to extraConfig:
#   source = ~/.config/hypr/debug.conf
###############################################################################
debug {
    disable_logs = false
    enable_stdout_logs = true
}
