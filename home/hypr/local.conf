# local.conf
# Host-specific or experimental overrides.
# This file is deployed but not sourced automatically — add it to extraConfig
# or source it manually from a Hyprland session for one-off tweaks.
